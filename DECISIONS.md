# 📋 القرارات المعمارية (ADR) — خدمة المصادقة والتفويض المركزية

سجل موجز لأهم القرارات التقنية التي تم اتخاذها في هذا المشروع، مع السياق والسبب والمقايضة (Trade-off) لكل قرار.

## القرار 01: اعتماد تدفق OAuth2 Authorization Code + PKCE للمصادقة

**السياق:** الحاجة لمصادقة موحّدة تدعم تطبيقات ويب وموبايل (Public Clients) بأمان دون تخزين Client Secret على الجهاز.

**القرار:** تم تطبيق تدفق Authorization Code مع PKCE، مع إصدار JWT قصيرة الأجل و Refresh Token مع دوران (Rotation).

**السبب:** PKCE يحمي من هجمات اعتراض كود التفويض (Authorization Code Interception) وهو المعيار الموصى به للعملاء العامة.

**المقايضة (Trade-off):** تعقيد إضافي في تدفق تسجيل الدخول من جهة الواجهة الأمامية، تمت معالجته عبر مكتبة OAuth موحّدة مشتركة بين التطبيقات.

---

## القرار 02: نموذج تحكم بالصلاحيات مختلط (RBAC + ABAC)

**السياق:** بعض القرارات تحتاج صلاحية على مستوى الدور (مدير، موظف) وبعضها يحتاج شروط سياقية (مثل: صاحب السجل فقط).

**القرار:** تم استخدام RBAC كطبقة أساسية لتحديد الصلاحيات العامة، مع طبقة ABAC فوقها للتحقق من شروط سياقية إضافية عند الحاجة.

**السبب:** هذا المزيج يوازن بين بساطة الإدارة (RBAC) ومرونة التحكم الدقيق (ABAC) دون تعقيد كل الصلاحيات بنفس المستوى.

**المقايضة (Trade-off):** تحقق إضافي في وقت الطلب (Runtime) للحالات التي تحتاج ABAC، تم تقليل أثره عبر التخزين المؤقت لنتائج التحقق.

---

## القرار 03: طبقة تخزين مؤقت (Caching) باستخدام Redis

**السياق:** جزء كبير من الطلبات يطلب نفس البيانات بشكل متكرر (قراءة أكثر من كتابة)، ما يشكّل ضغطاً غير ضروري على قاعدة البيانات.

**القرار:** تم إدخال Redis كطبقة تخزين مؤقت أمام قاعدة البيانات مع استراتيجية Cache-Aside وانتهاء صلاحية (TTL) مضبوط حسب نوع البيانات.

**السبب:** خفّض هذا زمن الاستجابة بشكل ملموس وقلّل الحمل عن قاعدة البيانات الرئيسية في أوقات الذروة.

**المقايضة (Trade-off):** احتمالية عرض بيانات قديمة لفترة قصيرة (Eventual Consistency)، وتم قبوله للبيانات غير الحرجة زمنياً مع إبطال فوري (Invalidation) للبيانات الحساسة.

---

## القرار 04: تحزيم كل الخدمات عبر Docker

**السياق:** الحاجة لبيئة تشغيل متطابقة بين التطوير والاختبار والإنتاج، وتسهيل النشر على بنى تحتية مختلفة.

**القرار:** تم تحزيم كل مكوّن من مكوّنات النظام كـ Docker Image منفصل مع Docker Compose للتطوير المحلي.

**السبب:** يلغي هذا مشكلة 'يعمل عندي بس' ويسهّل النشر على أي بيئة تدعم الحاويات (سحابية أو محلية).

**المقايضة (Trade-off):** تكلفة تعلّم إضافية بسيطة للفريق غير المعتاد على الحاويات، عولجت عبر توثيق داخلي مختصر لأوامر التشغيل الشائعة.

---

## القرار 05: بناء خط CI/CD آلي

**السياق:** النشر اليدوي المتكرر كان يستهلك وقتاً وعرضة لأخطاء بشرية عند كل تحديث.

**القرار:** تم إعداد خط أنابيب آلي (بناء → اختبار → نشر) يعمل تلقائياً عند كل Push على الفرع الرئيسي.

**السبب:** يقلل هذا زمن الوصول للتغييرات إلى الإنتاج (Lead Time) ويضمن تشغيل نفس مجموعة الاختبارات قبل كل نشر.

**المقايضة (Trade-off):** حاجة لضبط بيئة الاختبار الآلي بعناية لتفادي False Positives، تم التعامل معها بتحسين الاختبارات تدريجياً.

---

*جزء من [توثيق المشروع الكامل](./PROJECT.md)*
